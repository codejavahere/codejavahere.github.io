# 为什么选择 Markdown

Markdown 对日记来说足够简单。

它是纯文本，写作时不会被复杂的编辑器打断；同时又可以自然地表达标题、列表、代码和引用。

```python
print("keep writing")
```

更重要的是，每一篇日记都是仓库里的一个独立文件，可以随着 Git 一起保存历史。

以后新增日记，只需要：

1. 在 `diary/` 创建一个 `.md` 文件。
2. 在 `diary/index.json` 增加一条元数据。
3. 推送到 GitHub Pages。

网站会自动把它显示到时间线上。
